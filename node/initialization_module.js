// I am almost certain that I need these:
var request = require('request');
var Promise = require('bluebird');

// I have no idea if I need these:
var _ = require('underscore');
var fs = require('fs');
var parse = require('parse-link-header');
var querystring = require('querystring');

// For open source projects tested on travis-ci.org, use https://api.travis-ci.org.

// Define method to check for Travis CI file.

// Define method to create Travis CI file.

// Define method to update Travis CI file.

// Define method to check for Coveralls file.

// Define method to create Coveralls file.

// Define method to update Coveralls file.

// Export methods for external use.